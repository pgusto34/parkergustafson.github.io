---
interface Props {
  eyebrow?: string;
  title: string;
  text: string;
}
const { eyebrow = "In progress", title, text } = Astro.props;
---

<div class="empty-state">
  <p class="eyebrow">{eyebrow}</p>
  <h2>{title}</h2>
  <p>{text}</p>
</div>
